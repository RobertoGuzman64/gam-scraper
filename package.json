{
  "name": "gam-scraper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "scrape": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "p-limit": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
